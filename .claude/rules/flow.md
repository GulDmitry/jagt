---
paths:
  - "**/dev/jagt/orchestrator/flow/**"
  - "**/dev/jagt/orchestrator/capability/**"
  - "**/FlowWiring.java"
---

Nothing below `flow/` names a status. `withStatus` lives in `flow/` and in the record, nowhere else.

Full rules for the flow machine: **`docs/rules/flow.md`** — read it before changing behaviour here.
